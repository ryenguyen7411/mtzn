import Entity from 'entity';
import Storage from 'infra/storage';

export default function Repo () {
  const entity = new Entity(); // eslint-disable-line
  const storage = new Storage(); // eslint-disable-line
}
